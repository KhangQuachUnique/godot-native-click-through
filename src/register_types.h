#pragma once

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void initialize_native_click_through_module(ModuleInitializationLevel p_level);
void uninitialize_native_click_through_module(ModuleInitializationLevel p_level);